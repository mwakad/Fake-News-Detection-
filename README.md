# Fake News Detection
Using machine learning to detect fake news in web-hosted articles. Leveraging logistic regression (Baseline), XGBoost (Ensemble model), LSTM (neural network) and NLP hugging face transformers (RoBERTa, BERT, Longformer)
